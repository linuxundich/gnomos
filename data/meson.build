install_data(
  'de.christophlangner.Gnomos.desktop',
  install_dir: get_option('datadir') / 'applications',
)

install_data(
  'de.christophlangner.Gnomos.metainfo.xml',
  install_dir: get_option('datadir') / 'metainfo',
)

install_data(
  'icons/hicolor/scalable/apps/de.christophlangner.Gnomos.svg',
  install_dir: get_option('datadir') / 'icons/hicolor/scalable/apps',
)

# NOTE: translations (i18n.merge_file() + po/) are not wired up yet — see
# README.md roadmap. The .desktop/.metainfo files below are installed as
# plain, untranslated data files for now.
